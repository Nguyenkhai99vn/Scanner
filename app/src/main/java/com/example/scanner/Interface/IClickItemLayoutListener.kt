package com.example.scanner.Interface

import com.example.scanner.Model.item_image
import com.example.scanner.Model.item_layout

interface IClickItemLayoutListener {
    fun clickItemLayout(itemLayout: item_layout)
}