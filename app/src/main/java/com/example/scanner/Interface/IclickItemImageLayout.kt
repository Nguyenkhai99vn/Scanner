package com.example.scanner.Interface

import com.example.scanner.Model.ModelToPDF.Layout

interface IclickItemImageLayout {
    fun clickImage(itemLayout : Layout){
    }
    fun clickEditText(itemLayout : Layout){

    }
}